#include "main.h"

/**
 * strcat - concatenate two string
 * @dest: buffer
 * @src: source
 *
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
	int i = 0, j = 0;

	while (dest[i++])
	{
		j++
	}
	
	while (src[i])
	{
		dest[j++] = src[i];i
		i++;
	}
	dest[i] = '\0';

	return (dest);
}
