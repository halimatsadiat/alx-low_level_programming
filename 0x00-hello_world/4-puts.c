#include <stdio.h>
#include <string.h>

/**
 
   * main - functiom
   * Return: 0 if done properly.
 */

int main(void)

{
		char myChar[45];
			strcpy(myChar, "\"Programming is like building a multilingual puzzle");
				puts(myChar);
					return (0);
}
