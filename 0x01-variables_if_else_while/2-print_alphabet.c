#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;
	printf("\nLowercase characters : \n");
	    for (ch = 'a'; ch <= 'z'; ch++)
	    {
		    printf("%c ", ch);
	    }
	    return (0);
}
